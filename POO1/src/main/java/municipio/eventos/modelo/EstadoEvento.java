package municipio.eventos.modelo;

public enum EstadoEvento {
    PLANIFICACION,
    CONFIRMADO,
    EN_EJECUCION,
    FINALIZADO
}
// Este enum representa los diferentes estados que un evento puede tener en el sistema.
// Cada estado indica una fase específica en el ciclo de vida del evento, desde su planificación inicial hasta su finalización.